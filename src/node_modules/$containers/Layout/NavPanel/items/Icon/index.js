import React from 'react';

import style from '../../NavPanel.module.css';
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';

/* WRAPPED IN ADDITIONAL DIV BEACAUSE onClick DOESN'T WORK PROPERLY */
const Icon = ({onClick, icon}) => {
  return (
    <div onClick={onClick}>
      <div className={style.panelItem}>
        <FontAwesomeIcon icon={icon}/>
      </div>
    </div>
  );
};

export default Icon;